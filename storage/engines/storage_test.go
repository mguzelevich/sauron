package storage

/*

import (
	"testing"
	"time"
)

// https://segment.com/blog/5-advanced-testing-techniques-in-go/
func TestStorage_meta(t *testing.T) {
	storage, _ := Mock()
	Storage = storage

	meta, err := Storage.Meta()
	if err != nil {
		t.Error(meta, err)
	}
}

func TestStorage_SaveTelemetry(t *testing.T) {
	storage, _ := Mock()
	Storage = storage

	timestamp := time.Now().UTC()
	telemetry := &common.Telemetry{
		Timestamp: &timestamp,
	}
	storage.SaveTelemetry(telemetry)
}
*/
